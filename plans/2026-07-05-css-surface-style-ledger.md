# CSS Surface Style Ledger

Date: 2026-07-05

## Purpose

This ledger maps the current `surgeist-css` authored API surface to explicit
`surgeist-style` ownership decisions. It is the source contract for later
style implementation plans and root lowering work.

## Source Snapshot

| Repo | Path | Commit | Status |
| --- | --- | --- | --- |
| style | `/Users/codex/Development/surgeist-style` | `5ac070a3d8270a7ec368023769b6b056853f8f57` | `ahead 1 with untracked inventory ledger implementation plan` |
| css | `/Users/codex/Development/surgeist-css` | `1c95d4218439f1696151e0ee9602671fab418314` | `clean` |

## Boundary Rules

- `surgeist-css` owns strict parsing, CSS syntax recovery policy, CSS source
  locations, and authored CSS syntax types.
- Root owns lowering from `surgeist-css` types into style front-door APIs and
  integration diagnostics for unsupported CSS.
- `surgeist-style` owns typed receiving models, cascade semantics, selector
  matching contracts over root-provided facts, inheritance, invalidation, and
  computed style outputs.
- `surgeist-style` must not depend on `surgeist-css`.
- Every CSS surface item in this ledger is classified as `Existing style model`,
  `New style model needed`, `Typed symbolic style data`, `Root rejection`,
  `Out of style boundary`, or `Root-owned lowering boundary`.

## Classification Labels

| Label | Meaning |
| --- | --- |
| `Existing style model` | Style already has a typed model or behavior that can receive this surface intentionally. |
| `New style model needed` | Style owns the semantic domain, but a new typed model or behavior must be added. |
| `Typed symbolic style data` | Style should preserve typed symbolic data until a later owner or context can resolve it. |
| `Root rejection` | Root should reject this parsed CSS surface for now with an unsupported-integration diagnostic. |
| `Out of style boundary` | The surface is parsed by CSS but belongs to another crate or host concern, not style. |
| `Root-owned lowering boundary` | Root must translate this CSS syntax into style-owned inputs; style must not import CSS types. |

## CSS Rule Ledger

| CSS rule | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- |
| `CssRule::Import` | `Out of style boundary` | `root` | Root owns stylesheet fetch ordering and import policy; style should only receive already-lowered rule inputs. | CSS surface inventory follow-up/root decision |
| `CssRule::LayerStatement` | `Existing style model` | `style` | `LayerStatement` and `LayerRegistry` receive named layer statements and assign style-owned cascade `LayerOrder` values. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `CssRule::LayerBlock` | `Existing style model` | `style` | `LayerBlock`, `LayerRegistry`, and sheet layer rule APIs receive named or anonymous layer blocks and attach their `LayerOrder` to rules. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `CssRule::FontFace` | `Out of style boundary` | `text` | Font loading and font-face descriptors remain text or root owned until a font pipeline exists. | CSS surface inventory follow-up/root decision |
| `CssRule::Keyframes` | `Typed symbolic style data` | `style` | Style should preserve named keyframe blocks as typed symbolic animation data. | Timing, animation, and keyframe style data |
| `CssRule::Style` | `Root-owned lowering boundary` | `root` | Root lowers parsed selectors and declarations into style-owned authored rule inputs without importing CSS types. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssRule::Media` | `Existing style model` | `style` | `Condition::Media(MediaQueryList)` receives root-lowered media query lists and evaluates them against `MediaEnvironment`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `CssRule::Container` | `Existing style model` | `style` | `Condition::Container(ContainerCondition)` receives root-lowered container queries and evaluates them against `ContainerFacts`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `CssRule::Scope` | `Existing style model` | `style` | `RuleScope` and `ScopeSelectorList` receive scope roots and limits, and resolver matching applies scoped rule anchors. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |

## CSS Declaration And Value Family Ledger

| CSS value family | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- |
| `CssValue::GlobalKeyword` | `New style model needed` | `style` | Root lowers parsed CSS-wide keywords into a style-owned receiving model. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssValue::CustomProperty` | `New style model needed` | `style` | Style needs a custom property store and typed authored-token preservation. | Custom properties and variable substitution |
| `CssValue::VariableDependent` | `New style model needed` | `style` | Style needs a variable-dependent declaration path that preserves authored values and references until substitution. | Custom properties and variable substitution |
| `CssValue::Display` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Display` validation. | Layout-facing property family expansion |
| `CssValue::BoxSizing` | `Existing style model` | `style` | Non-variable values lower to existing `Value::BoxSizing` validation. | Layout-facing property family expansion |
| `CssValue::Position` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Position` validation. | Layout-facing property family expansion |
| `CssValue::Direction` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Direction` validation. | Layout-facing property family expansion |
| `CssValue::Overflow` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Overflow` validation. | Layout-facing property family expansion |
| `CssValue::OverflowAxes` | `Existing style model` | `style` | Non-variable shorthand values lower to existing `Value::OverflowAxes` validation. | Layout-facing property family expansion |
| `CssValue::FlexDirection` | `Existing style model` | `style` | Non-variable values lower to existing `Value::FlexDirection` validation. | Layout-facing property family expansion |
| `CssValue::FlexWrap` | `Existing style model` | `style` | Non-variable values lower to existing `Value::FlexWrap` validation. | Layout-facing property family expansion |
| `CssValue::Float` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Float` validation. | Layout-facing property family expansion |
| `CssValue::Clear` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Clear` validation. | Layout-facing property family expansion |
| `CssValue::Alignment` | `Existing style model` | `style` | Non-variable values lower to existing align-content or justify-content style values. | Layout-facing property family expansion |
| `CssValue::AlignItems` | `Existing style model` | `style` | Non-variable values lower to existing `Value::AlignItems` validation. | Layout-facing property family expansion |
| `CssValue::PlaceAlignment` | `Existing style model` | `style` | Shorthand values can lower to existing align and justify longhand models. | Layout-facing property family expansion |
| `CssValue::Visibility` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Visibility` validation. | Layout-facing property family expansion |
| `CssValue::Content` | `New style model needed` | `style` | Style needs generated content receiving data. | Generated content, counters, lists, and marker policy |
| `CssValue::ContentVisibility` | `New style model needed` | `style` | Style lacks a typed content-visibility model. | Layout-facing property family expansion |
| `CssValue::ListStyleType` | `New style model needed` | `style` | Style needs list marker type data. | Generated content, counters, lists, and marker policy |
| `CssValue::ListStylePosition` | `New style model needed` | `style` | Style needs list marker position data. | Generated content, counters, lists, and marker policy |
| `CssValue::ListStyleImage` | `Out of style boundary` | `render` | Marker image loading belongs to render or host integration, not core style semantics. | Generated content, counters, lists, and marker policy |
| `CssValue::ListStyle` | `New style model needed` | `style` | Style needs shorthand lowering into list marker longhands. | Generated content, counters, lists, and marker policy |
| `CssValue::CounterChanges` | `New style model needed` | `style` | Style needs counter reset, increment, and set data. | Generated content, counters, lists, and marker policy |
| `CssValue::Length` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Length` validation. | Layout-facing property family expansion |
| `CssValue::GridFlowTolerance` | `Existing style model` | `style` | Non-variable values lower to existing `Value::GridFlowTolerance` validation. | Layout-facing property family expansion |
| `CssValue::GridTrackList` | `Existing style model` | `style` | Non-variable values lower to existing `Value::GridTrackList` validation. | Layout-facing property family expansion |
| `CssValue::GridTemplateAreas` | `Existing style model` | `style` | Non-variable values lower to existing `Value::GridTemplateAreas` validation. | Layout-facing property family expansion |
| `CssValue::GridTemplate` | `Existing style model` | `style` | Non-variable values lower to existing `Value::GridTemplate` validation. | Layout-facing property family expansion |
| `CssValue::GridAutoFlow` | `Existing style model` | `style` | Non-variable values lower to existing `Value::GridAutoFlow` validation. | Layout-facing property family expansion |
| `CssValue::GridLine` | `Existing style model` | `style` | Non-variable values lower to existing `Value::GridLine` validation. | Layout-facing property family expansion |
| `CssValue::GridLineRange` | `Existing style model` | `style` | Shorthand values can lower to existing `Value::GridPlacement` validation. | Layout-facing property family expansion |
| `CssValue::GridArea` | `Existing style model` | `style` | Shorthand values can lower to existing `Value::GridAreaPlacement` validation. | Layout-facing property family expansion |
| `CssValue::Grid` | `Existing style model` | `style` | Shorthand values can lower to existing `Value::GridDefinition` validation. | Layout-facing property family expansion |
| `CssValue::WritingMode` | `Existing style model` | `style` | Non-variable values lower to existing `Value::WritingMode` validation. | Layout-facing property family expansion |
| `CssValue::TextAlign` | `Existing style model` | `style` | Non-variable values lower to existing `Value::TextAlign` validation. | Text-facing property family expansion |
| `CssValue::TextAlignLast` | `New style model needed` | `style` | Style lacks a typed text-align-last value and property path. | Text-facing property family expansion |
| `CssValue::TextIndent` | `New style model needed` | `style` | Style lacks a typed text-indent value and property path. | Text-facing property family expansion |
| `CssValue::VerticalAlign` | `New style model needed` | `style` | Style lacks a typed vertical-align value and property path. | Text-facing property family expansion |
| `CssValue::FontFamily` | `Existing style model` | `style` | Non-variable values lower to existing `Value::FontFamilyList` validation. | Text-facing property family expansion |
| `CssValue::Font` | `New style model needed` | `style` | Style needs font shorthand lowering across font longhands, including missing typed font models. | Text-facing property family expansion |
| `CssValue::FontWeight` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::FontStyle` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::FontStretch` | `New style model needed` | `style` | Style lacks a typed font-stretch model. | Text-facing property family expansion |
| `CssValue::FontVariant` | `New style model needed` | `style` | Style lacks a typed font-variant model. | Text-facing property family expansion |
| `CssValue::FontFeatureSettings` | `New style model needed` | `style` | Style lacks typed font feature settings data. | Text-facing property family expansion |
| `CssValue::LetterSpacing` | `New style model needed` | `style` | Style lacks a typed letter-spacing model. | Text-facing property family expansion |
| `CssValue::TextWrap` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::WhiteSpace` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::WordBreak` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::OverflowWrap` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::TextOverflow` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Text-facing property family expansion |
| `CssValue::TextDecoration` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed text decoration model. | Text-facing property family expansion |
| `CssValue::TextDecorationLine` | `New style model needed` | `style` | Style lacks a typed text-decoration-line model. | Text-facing property family expansion |
| `CssValue::TextDecorationColor` | `New style model needed` | `style` | Style lacks a text-decoration-color property path even though color values exist. | Text-facing property family expansion |
| `CssValue::TextDecorationStyle` | `New style model needed` | `style` | Style lacks a typed text-decoration-style model. | Text-facing property family expansion |
| `CssValue::TextDecorationThickness` | `New style model needed` | `style` | Style lacks a typed text-decoration-thickness model. | Text-facing property family expansion |
| `CssValue::TextTransform` | `New style model needed` | `style` | Style lacks a typed text-transform model. | Text-facing property family expansion |
| `CssValue::Edges` | `Existing style model` | `style` | Non-variable shorthand values lower to existing `Value::Edges` validation. | Layout-facing property family expansion |
| `CssValue::Color` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Color` validation where the property path exists. | Paint, color, and effect property family expansion |
| `CssValue::ZIndex` | `Existing style model` | `style` | Non-variable values lower to existing numeric z-index validation. | Layout-facing property family expansion |
| `CssValue::BoxDecorationBreak` | `New style model needed` | `style` | Style lacks a typed box-decoration-break model. | Paint, color, and effect property family expansion |
| `CssValue::Border` | `New style model needed` | `style` | Style needs border shorthand decomposition across width, style, and color models. | Paint, color, and effect property family expansion |
| `CssValue::BorderStyle` | `New style model needed` | `style` | Style metadata currently uses `Value::Keyword(Keyword::Initial)` and lacks a typed model. | Paint, color, and effect property family expansion |
| `CssValue::BorderStyles` | `New style model needed` | `style` | Style lacks typed multi-side border-style data. | Paint, color, and effect property family expansion |
| `CssValue::BackgroundImage` | `Out of style boundary` | `render` | Image resource loading belongs to render or host integration. | Paint, color, and effect property family expansion |
| `CssValue::BackgroundPosition` | `New style model needed` | `style` | Style lacks typed background-position layer data. | Paint, color, and effect property family expansion |
| `CssValue::BackgroundSize` | `New style model needed` | `style` | Style lacks typed background-size layer data. | Paint, color, and effect property family expansion |
| `CssValue::BackgroundRepeat` | `New style model needed` | `style` | Style lacks typed background-repeat layer data. | Paint, color, and effect property family expansion |
| `CssValue::BackgroundBox` | `New style model needed` | `style` | Style lacks typed background origin and clip box data. | Paint, color, and effect property family expansion |
| `CssValue::BackgroundAttachment` | `New style model needed` | `style` | Style lacks typed background-attachment layer data. | Paint, color, and effect property family expansion |
| `CssValue::BorderRadius` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Corners` validation for all corners. | Paint, color, and effect property family expansion |
| `CssValue::CornerRadius` | `New style model needed` | `style` | Style lacks individual corner-radius longhand storage. | Paint, color, and effect property family expansion |
| `CssValue::BoxShadow` | `Existing style model` | `style` | Non-variable values lower to existing `Value::ShadowList` validation. | Paint, color, and effect property family expansion |
| `CssValue::Opacity` | `Existing style model` | `style` | Non-variable values lower to existing numeric opacity validation. | Paint, color, and effect property family expansion |
| `CssValue::FlexGrow` | `Existing style model` | `style` | Non-variable values lower to existing numeric flex-grow validation. | Layout-facing property family expansion |
| `CssValue::FlexShrink` | `Existing style model` | `style` | Non-variable values lower to existing numeric flex-shrink validation. | Layout-facing property family expansion |
| `CssValue::Order` | `New style model needed` | `style` | Style lacks a typed order property model. | Layout-facing property family expansion |
| `CssValue::Flex` | `Existing style model` | `style` | Shorthand values can lower to existing flex-grow, flex-shrink, and flex-basis models. | Layout-facing property family expansion |
| `CssValue::AspectRatio` | `Existing style model` | `style` | Non-variable values lower to existing numeric aspect-ratio validation. | Layout-facing property family expansion |
| `CssValue::ScrollbarWidth` | `Existing style model` | `style` | Non-variable values lower to existing numeric scrollbar-width validation. | Layout-facing property family expansion |
| `CssValue::Cursor` | `Existing style model` | `style` | Non-variable values lower to existing `Value::Cursor` validation. | Paint, color, and effect property family expansion |
| `CssValue::PointerEvents` | `Existing style model` | `style` | Non-variable values lower to existing `Value::PointerEvents` validation. | Paint, color, and effect property family expansion |
| `CssValue::UserSelect` | `New style model needed` | `style` | Style lacks a typed user-select model. | Text-facing property family expansion |
| `CssValue::Outline` | `New style model needed` | `style` | Style lacks CSS outline shorthand data distinct from focus outline. | Paint, color, and effect property family expansion |
| `CssValue::OutlineColor` | `New style model needed` | `style` | Style lacks a CSS outline-color property path distinct from focus outline. | Paint, color, and effect property family expansion |
| `CssValue::OutlineStyle` | `New style model needed` | `style` | Style lacks a typed CSS outline-style model. | Paint, color, and effect property family expansion |
| `CssValue::OutlineWidth` | `New style model needed` | `style` | Style lacks a CSS outline-width property path distinct from focus outline. | Paint, color, and effect property family expansion |
| `CssValue::Transform` | `Existing style model` | `style` | Non-variable transform lists lower to existing `Value::Transform` validation. | Paint, color, and effect property family expansion |
| `CssValue::TransformOrigin` | `Existing style model` | `style` | Non-variable positions can lower to existing `Value::Size` transform-origin validation. | Paint, color, and effect property family expansion |
| `CssValue::Translate` | `New style model needed` | `style` | Style lacks individual transform longhand data. | Paint, color, and effect property family expansion |
| `CssValue::Rotate` | `New style model needed` | `style` | Style lacks individual transform longhand data. | Paint, color, and effect property family expansion |
| `CssValue::Scale` | `New style model needed` | `style` | Style lacks individual transform longhand data. | Paint, color, and effect property family expansion |
| `CssValue::Filter` | `Typed symbolic style data` | `style` | Preserve authored filter function data until a typed effect model is added. | Paint, color, and effect property family expansion |
| `CssValue::ClipPath` | `Typed symbolic style data` | `style` | Preserve basic shape and clip-path symbolic data until paint integration resolves it. | Paint, color, and effect property family expansion |
| `CssValue::Mask` | `Out of style boundary` | `render` | Mask image resource loading belongs to render or host integration. | Paint, color, and effect property family expansion |
| `CssValue::MaskImage` | `Out of style boundary` | `render` | Mask image resource loading belongs to render or host integration. | Paint, color, and effect property family expansion |
| `CssValue::MaskSize` | `New style model needed` | `style` | Style lacks typed mask-size layer data. | Paint, color, and effect property family expansion |
| `CssValue::MaskPosition` | `New style model needed` | `style` | Style lacks typed mask-position layer data. | Paint, color, and effect property family expansion |
| `CssValue::MaskRepeat` | `New style model needed` | `style` | Style lacks typed mask-repeat layer data. | Paint, color, and effect property family expansion |
| `CssValue::TransitionProperty` | `Existing style model` | `style` | Non-variable values lower to existing `Value::PropertyList` validation. | Timing, animation, and keyframe style data |
| `CssValue::TimeList` | `New style model needed` | `style` | Style has scalar timing metadata but lacks CSS time-list models. | Timing, animation, and keyframe style data |
| `CssValue::EasingList` | `Typed symbolic style data` | `style` | Preserve easing function data until a typed timing function model is added. | Timing, animation, and keyframe style data |
| `CssValue::Transition` | `New style model needed` | `style` | Style needs transition shorthand decomposition across property, duration, delay, and easing. | Timing, animation, and keyframe style data |
| `CssValue::AnimationName` | `Existing style model` | `style` | Non-variable values lower to existing `Value::AnimationNameList` validation. | Timing, animation, and keyframe style data |
| `CssValue::AnimationIterationCount` | `New style model needed` | `style` | Style lacks animation iteration-count list data. | Timing, animation, and keyframe style data |
| `CssValue::AnimationDirection` | `New style model needed` | `style` | Style lacks animation direction list data. | Timing, animation, and keyframe style data |
| `CssValue::AnimationFillMode` | `New style model needed` | `style` | Style lacks animation fill-mode list data. | Timing, animation, and keyframe style data |
| `CssValue::AnimationPlayState` | `New style model needed` | `style` | Style lacks animation play-state list data. | Timing, animation, and keyframe style data |
| `CssValue::Animation` | `New style model needed` | `style` | Style needs animation shorthand decomposition across name, timing, counts, direction, fill, and play state. | Timing, animation, and keyframe style data |

## CSS-Wide Keyword Ledger

| CSS-wide keyword | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- |
| `CssGlobalKeyword::Inherit` | `New style model needed` | `style` | Root lowers the parsed keyword into style-owned CSS-wide keyword data; style owns property-aware resolution against inherited values, initial values, cascade origin, and layer state. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssGlobalKeyword::Initial` | `New style model needed` | `style` | Root lowers the parsed keyword into style-owned CSS-wide keyword data; style owns property-aware resolution against inherited values, initial values, cascade origin, and layer state. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssGlobalKeyword::Unset` | `New style model needed` | `style` | Root lowers the parsed keyword into style-owned CSS-wide keyword data; style owns property-aware resolution against inherited values, initial values, cascade origin, and layer state. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssGlobalKeyword::Revert` | `New style model needed` | `style` | Root lowers the parsed keyword into style-owned CSS-wide keyword data; style owns property-aware resolution against inherited values, initial values, cascade origin, and layer state. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssGlobalKeyword::RevertLayer` | `New style model needed` | `style` | Root lowers the parsed keyword into style-owned CSS-wide keyword data; style owns property-aware resolution against inherited values, initial values, cascade origin, and layer state. | Authored declarations, cascade metadata, and CSS-wide keywords |

## CSS Property Ledger

| CSS property | CSS value family | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- | --- |
| `CssProperty::All` | `CssValue::GlobalKeyword` | `New style model needed` | `style` | Supported only for CSS-wide keywords; style needs an all-property cascade reset model, and `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Authored declarations, cascade metadata, and CSS-wide keywords |
| `CssProperty::Display` | `CssValue::Display` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Display`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::BoxSizing` | `CssValue::BoxSizing` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::BoxSizing`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Position` | `CssValue::Position` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Position`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Direction` | `CssValue::Direction` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Direction`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Overflow` | `CssValue::Overflow` or `CssValue::OverflowAxes` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing overflow models, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::OverflowX` | `CssValue::Overflow` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::OverflowX`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::OverflowY` | `CssValue::Overflow` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::OverflowY`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::FlexDirection` | `CssValue::FlexDirection` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FlexDirection`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::FlexWrap` | `CssValue::FlexWrap` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FlexWrap`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Float` | `CssValue::Float` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Float`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Clear` | `CssValue::Clear` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Clear`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::AlignContent` | `CssValue::Alignment` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::AlignContent`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::JustifyContent` | `CssValue::Alignment` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::JustifyContent`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::AlignItems` | `CssValue::AlignItems` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::AlignItems`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::AlignSelf` | `CssValue::AlignItems` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::AlignSelf`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::JustifyItems` | `CssValue::AlignItems` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::JustifyItems`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::JustifySelf` | `CssValue::AlignItems` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::JustifySelf`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::PlaceContent` | `CssValue::PlaceAlignment` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing align-content and justify-content models, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::PlaceItems` | `CssValue::PlaceAlignment` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing align-items and justify-items models, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::PlaceSelf` | `CssValue::PlaceAlignment` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing align-self and justify-self models, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Visibility` | `CssValue::Visibility` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Visibility`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Content` | `CssValue::Content` | `New style model needed` | `style` | Supported CSS property; style lacks a typed generated-content model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::ContentVisibility` | `CssValue::ContentVisibility` | `New style model needed` | `style` | Supported CSS property; style lacks a typed content-visibility model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::ListStyleType` | `CssValue::ListStyleType` | `New style model needed` | `style` | Supported CSS property; style lacks a typed list-style-type model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::ListStylePosition` | `CssValue::ListStylePosition` | `New style model needed` | `style` | Supported CSS property; style lacks a typed list-style-position model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::ListStyleImage` | `CssValue::ListStyleImage` | `New style model needed` | `style` | Supported CSS property; style lacks list marker image receiving data while render owns resource loading, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::ListStyle` | `CssValue::ListStyle` | `New style model needed` | `style` | Supported shorthand; style lacks list-style longhand models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::CounterReset` | `CssValue::CounterChanges` | `New style model needed` | `style` | Supported CSS property; style lacks counter mutation data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::CounterIncrement` | `CssValue::CounterChanges` | `New style model needed` | `style` | Supported CSS property; style lacks counter mutation data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::CounterSet` | `CssValue::CounterChanges` | `New style model needed` | `style` | Supported CSS property; style lacks counter mutation data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Generated content, counters, lists, and marker policy |
| `CssProperty::Width` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Width`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Height` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Height`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::MinWidth` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::MinWidth`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::MinHeight` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::MinHeight`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::MaxWidth` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::MaxWidth`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::MaxHeight` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::MaxHeight`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::FlexBasis` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FlexBasis`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Gap` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Gap`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::RowGap` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::RowGap`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::ColumnGap` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::ColumnGap`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridFlowTolerance` | `CssValue::GridFlowTolerance` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridFlowTolerance`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridTemplateRows` | `CssValue::GridTrackList` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridTemplateRows`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridTemplateColumns` | `CssValue::GridTrackList` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridTemplateColumns`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridTemplateAreas` | `CssValue::GridTemplateAreas` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridTemplateAreas`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridTemplate` | `CssValue::GridTemplate` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing grid template models, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridAutoRows` | `CssValue::GridTrackList` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridAutoRows`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridAutoColumns` | `CssValue::GridTrackList` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridAutoColumns`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridAutoFlow` | `CssValue::GridAutoFlow` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridAutoFlow`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridRowStart` | `CssValue::GridLine` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridRowStart`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridRowEnd` | `CssValue::GridLine` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridRowEnd`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridColumnStart` | `CssValue::GridLine` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridColumnStart`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridColumnEnd` | `CssValue::GridLine` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::GridColumnEnd`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridRow` | `CssValue::GridLineRange` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::GridRow`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridColumn` | `CssValue::GridLineRange` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::GridColumn`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::GridArea` | `CssValue::GridArea` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::GridArea`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Grid` | `CssValue::Grid` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::Grid`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::FontSize` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FontSize`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Text-facing property family expansion |
| `CssProperty::LineHeight` | `CssValue::Length` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::LineHeight`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Text-facing property family expansion |
| `CssProperty::WritingMode` | `CssValue::WritingMode` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::WritingMode`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::TextAlign` | `CssValue::TextAlign` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::TextAlign`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Text-facing property family expansion |
| `CssProperty::TextAlignLast` | `CssValue::TextAlignLast` | `New style model needed` | `style` | Supported CSS property; style lacks a typed text-align-last model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextIndent` | `CssValue::TextIndent` | `New style model needed` | `style` | Supported CSS property; style lacks a typed text-indent model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::VerticalAlign` | `CssValue::VerticalAlign` | `New style model needed` | `style` | Supported CSS property; style lacks a typed vertical-align model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::FontFamily` | `CssValue::FontFamily` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FontFamily`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Text-facing property family expansion |
| `CssProperty::Font` | `CssValue::Font` | `New style model needed` | `style` | Supported shorthand; style lacks typed models for several font longhands, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::FontWeight` | `CssValue::FontWeight` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::FontStyle` | `CssValue::FontStyle` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::FontStretch` | `CssValue::FontStretch` | `New style model needed` | `style` | Supported CSS property; style lacks a typed font-stretch model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::FontVariant` | `CssValue::FontVariant` | `New style model needed` | `style` | Supported CSS property; style lacks a typed font-variant model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::FontFeatureSettings` | `CssValue::FontFeatureSettings` | `New style model needed` | `style` | Supported CSS property; style lacks typed font feature settings, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::LetterSpacing` | `CssValue::LetterSpacing` | `New style model needed` | `style` | Supported CSS property; style lacks a typed letter-spacing model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextWrap` | `CssValue::TextWrap` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::WhiteSpace` | `CssValue::WhiteSpace` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::WordBreak` | `CssValue::WordBreak` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::OverflowWrap` | `CssValue::OverflowWrap` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextOverflow` | `CssValue::TextOverflow` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextDecoration` | `CssValue::TextDecoration` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextDecorationLine` | `CssValue::TextDecorationLine` | `New style model needed` | `style` | Supported CSS property; style lacks a typed text-decoration-line model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextDecorationColor` | `CssValue::TextDecorationColor` | `New style model needed` | `style` | Supported CSS property; style lacks a text-decoration-color path, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextDecorationStyle` | `CssValue::TextDecorationStyle` | `New style model needed` | `style` | Supported CSS property; style lacks a typed text-decoration-style model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextDecorationThickness` | `CssValue::TextDecorationThickness` | `New style model needed` | `style` | Supported CSS property; style lacks a typed text-decoration-thickness model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::TextTransform` | `CssValue::TextTransform` | `New style model needed` | `style` | Supported CSS property; style lacks a typed text-transform model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::Inset` | `CssValue::Edges` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::Inset`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Top` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual inset longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::Right` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual inset longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::Bottom` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual inset longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::Left` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual inset longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::ZIndex` | `CssValue::ZIndex` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing numeric `Property::ZIndex`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::BoxDecorationBreak` | `CssValue::BoxDecorationBreak` | `New style model needed` | `style` | Supported CSS property; style lacks a typed box-decoration-break model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Margin` | `CssValue::Edges` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::Margin`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::MarginTop` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual margin longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::MarginRight` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual margin longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::MarginBottom` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual margin longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::MarginLeft` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual margin longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::Padding` | `CssValue::Edges` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::Padding`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::PaddingTop` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual padding longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::PaddingRight` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual padding longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::PaddingBottom` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual padding longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::PaddingLeft` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual padding longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::Border` | `CssValue::Border` | `New style model needed` | `style` | Supported shorthand; style lacks full border shorthand decomposition and typed border-style data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderTop` | `CssValue::Border` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderRight` | `CssValue::Border` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderBottom` | `CssValue::Border` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderLeft` | `CssValue::Border` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderWidth` | `CssValue::Edges` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::BorderWidth`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::BorderTopWidth` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual border-width longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderRightWidth` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual border-width longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderBottomWidth` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual border-width longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderLeftWidth` | `CssValue::Length` | `New style model needed` | `style` | Supported CSS property; style lacks individual border-width longhand storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Color` | `CssValue::Color` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Color`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::Background` | `CssValue::Color` | `Existing style model` | `style` | Supported in current CSS parser as color-only background; root lowers to existing `Property::Background`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundColor` | `CssValue::Color` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing background color storage, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::BorderColor` | `CssValue::Color` | `Existing style model` | `style` | Supported in current CSS parser as a single color; root lowers to existing `Property::BorderColor`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::BorderTopColor` | `CssValue::Color` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border color storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderRightColor` | `CssValue::Color` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border color storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderBottomColor` | `CssValue::Color` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border color storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderLeftColor` | `CssValue::Color` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific border color storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundImage` | `CssValue::BackgroundImage` | `New style model needed` | `style` | Supported CSS property; style lacks background image layer receiving data while render owns resource loading, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundPosition` | `CssValue::BackgroundPosition` | `New style model needed` | `style` | Supported CSS property; style lacks background-position layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundSize` | `CssValue::BackgroundSize` | `New style model needed` | `style` | Supported CSS property; style lacks background-size layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundRepeat` | `CssValue::BackgroundRepeat` | `New style model needed` | `style` | Supported CSS property; style lacks background-repeat layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundOrigin` | `CssValue::BackgroundBox` | `New style model needed` | `style` | Supported CSS property; style lacks background-origin box data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundClip` | `CssValue::BackgroundBox` | `New style model needed` | `style` | Supported CSS property; style lacks background-clip box data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackgroundAttachment` | `CssValue::BackgroundAttachment` | `New style model needed` | `style` | Supported CSS property; style lacks background-attachment layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderStyle` | `CssValue::BorderStyles` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderTopStyle` | `CssValue::BorderStyle` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific typed border-style storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderRightStyle` | `CssValue::BorderStyle` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific typed border-style storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderBottomStyle` | `CssValue::BorderStyle` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific typed border-style storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderLeftStyle` | `CssValue::BorderStyle` | `New style model needed` | `style` | Supported CSS property; style lacks side-specific typed border-style storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderRadius` | `CssValue::BorderRadius` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing `Property::Radius`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::BorderTopLeftRadius` | `CssValue::CornerRadius` | `New style model needed` | `style` | Supported CSS property; style lacks individual corner-radius storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderTopRightRadius` | `CssValue::CornerRadius` | `New style model needed` | `style` | Supported CSS property; style lacks individual corner-radius storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderBottomRightRadius` | `CssValue::CornerRadius` | `New style model needed` | `style` | Supported CSS property; style lacks individual corner-radius storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BorderBottomLeftRadius` | `CssValue::CornerRadius` | `New style model needed` | `style` | Supported CSS property; style lacks individual corner-radius storage, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BoxShadow` | `CssValue::BoxShadow` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Shadow`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::Opacity` | `CssValue::Opacity` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Opacity`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::FlexGrow` | `CssValue::FlexGrow` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FlexGrow`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::FlexShrink` | `CssValue::FlexShrink` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::FlexShrink`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Order` | `CssValue::Order` | `New style model needed` | `style` | Supported CSS property; style lacks a typed order model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::Flex` | `CssValue::Flex` | `Existing style model` | `style` | Supported shorthand; root lowers non-variable values to existing flex-grow, flex-shrink, and flex-basis models, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::JustifyTracks` | `CssValue::Alignment` | `New style model needed` | `style` | Supported CSS property; style lacks track-alignment models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::AlignTracks` | `CssValue::Alignment` | `New style model needed` | `style` | Supported CSS property; style lacks track-alignment models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Layout-facing property family expansion |
| `CssProperty::AspectRatio` | `CssValue::AspectRatio` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::AspectRatio`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::ScrollbarWidth` | `CssValue::ScrollbarWidth` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::ScrollbarWidth`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Layout-facing property family expansion |
| `CssProperty::Cursor` | `CssValue::Cursor` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Cursor`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::PointerEvents` | `CssValue::PointerEvents` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::PointerEvents`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::UserSelect` | `CssValue::UserSelect` | `New style model needed` | `style` | Supported CSS property; style lacks a typed user-select model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Text-facing property family expansion |
| `CssProperty::Outline` | `CssValue::Outline` | `New style model needed` | `style` | Supported CSS property; style lacks CSS outline models distinct from focus outline, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::OutlineColor` | `CssValue::OutlineColor` | `New style model needed` | `style` | Supported CSS property; style lacks CSS outline-color storage distinct from focus outline, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::OutlineStyle` | `CssValue::OutlineStyle` | `New style model needed` | `style` | Supported CSS property; style lacks a typed outline-style model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::OutlineWidth` | `CssValue::OutlineWidth` | `New style model needed` | `style` | Supported CSS property; style lacks CSS outline-width storage distinct from focus outline, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Transform` | `CssValue::Transform` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::Transform`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::TransformOrigin` | `CssValue::TransformOrigin` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::TransformOrigin`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Paint, color, and effect property family expansion |
| `CssProperty::Translate` | `CssValue::Translate` | `New style model needed` | `style` | Supported CSS property; style lacks individual transform longhand models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Rotate` | `CssValue::Rotate` | `New style model needed` | `style` | Supported CSS property; style lacks individual transform longhand models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Scale` | `CssValue::Scale` | `New style model needed` | `style` | Supported CSS property; style lacks individual transform longhand models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Filter` | `CssValue::Filter` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed filter model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::BackdropFilter` | `CssValue::Filter` | `New style model needed` | `style` | Supported CSS property; style lacks a typed backdrop-filter model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::ClipPath` | `CssValue::ClipPath` | `New style model needed` | `style` | Supported CSS property; style lacks typed clip-path data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::Mask` | `CssValue::Mask` | `New style model needed` | `style` | Supported shorthand; style lacks mask layer receiving data while render owns resource loading, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::MaskImage` | `CssValue::MaskImage` | `New style model needed` | `style` | Supported CSS property; style lacks mask-image receiving data while render owns resource loading, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::MaskSize` | `CssValue::MaskSize` | `New style model needed` | `style` | Supported CSS property; style lacks mask-size layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::MaskPosition` | `CssValue::MaskPosition` | `New style model needed` | `style` | Supported CSS property; style lacks mask-position layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::MaskRepeat` | `CssValue::MaskRepeat` | `New style model needed` | `style` | Supported CSS property; style lacks mask-repeat layer data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Paint, color, and effect property family expansion |
| `CssProperty::TransitionProperty` | `CssValue::TransitionProperty` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::TransitionProperty`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Timing, animation, and keyframe style data |
| `CssProperty::TransitionDuration` | `CssValue::TimeList` | `New style model needed` | `style` | Supported CSS property; style has scalar timing metadata but lacks CSS time-list receiving data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::TransitionDelay` | `CssValue::TimeList` | `New style model needed` | `style` | Supported CSS property; style has scalar timing metadata but lacks CSS time-list receiving data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::TransitionTimingFunction` | `CssValue::EasingList` | `New style model needed` | `style` | Supported CSS property; style metadata uses `Value::Keyword(Keyword::Initial)` and lacks a typed easing model, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::Transition` | `CssValue::Transition` | `New style model needed` | `style` | Supported shorthand; style lacks transition shorthand and list models, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationName` | `CssValue::AnimationName` | `Existing style model` | `style` | Supported; root lowers non-variable values to existing `Property::AnimationName`, while `var(...)` still enters the `CssValue::VariableDependent` receiving path as `New style model needed`. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationDuration` | `CssValue::TimeList` | `New style model needed` | `style` | Supported CSS property; style lacks animation time-list data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationDelay` | `CssValue::TimeList` | `New style model needed` | `style` | Supported CSS property; style lacks animation time-list data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationTimingFunction` | `CssValue::EasingList` | `New style model needed` | `style` | Supported CSS property; style lacks animation easing-list data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationIterationCount` | `CssValue::AnimationIterationCount` | `New style model needed` | `style` | Supported CSS property; style lacks animation iteration-count data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationDirection` | `CssValue::AnimationDirection` | `New style model needed` | `style` | Supported CSS property; style lacks animation direction data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationFillMode` | `CssValue::AnimationFillMode` | `New style model needed` | `style` | Supported CSS property; style lacks animation fill-mode data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::AnimationPlayState` | `CssValue::AnimationPlayState` | `New style model needed` | `style` | Supported CSS property; style lacks animation play-state data, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::Animation` | `CssValue::Animation` | `New style model needed` | `style` | Supported shorthand; style lacks animation shorthand data beyond names, and `var(...)` also needs the `CssValue::VariableDependent` receiving path. | Timing, animation, and keyframe style data |
| `CssProperty::Custom(CssCustomPropertyName)` | `CssValue::CustomProperty` | `New style model needed` | `style` | Custom property declarations need a style-owned custom property store, CSS-wide keyword handling, and later variable substitution. | Custom properties and variable substitution |

## Selector And Tree Fact Ledger

| CSS selector surface | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- |
| Tag selectors | `Existing style model` | `style` | `Selector::Tag` and `Node::tag` can receive simple CSS type selectors, but root still must lower CSS tag identifiers and any namespace semantics are unsupported. | Selector lowering and matching integration |
| Key selectors | `Existing style model` | `style` | `Selector::Key` and `Node::key` can receive CSS ID selectors, but CSS ID uniqueness and document-specific semantics remain root/integration concerns. | Selector lowering and matching integration |
| Class selectors | `Existing style model` | `style` | `Selector::Class` and `Node::classes` can receive simple class selectors, but root still lowers CSS class tokens into style classes. | Selector lowering and matching integration |
| Compound selectors | `Existing style model` | `style` | `Compound` can combine tag, key, classes, states, simple attributes, and one position selector, but it lacks CSS pseudo-element, selector-list pseudo-class, type-filtered structural, and attribute matcher semantics. | Selector lowering and matching integration |
| Complex selectors | `Existing style model` | `style` | `Selector::Complex(Vec<Part>)` can receive ordered compound selector parts, but root must translate CSS subject ordering and unsupported pseudo-element/relative-selector semantics. | Selector lowering and matching integration |
| Descendant combinator | `Existing style model` | `style` | `Combinator::Descendant` and tree parent traversal support descendant matching over root-provided tree facts. | Selector lowering and matching integration |
| Child combinator | `Existing style model` | `style` | `Combinator::Child` and tree parent traversal support direct-child matching over root-provided tree facts. | Selector lowering and matching integration |
| Adjacent sibling combinator | `Existing style model` | `style` | `Combinator::Adjacent` and `Tree::previous_sibling` support adjacent sibling matching over root-provided tree facts. | Selector lowering and matching integration |
| Subsequent sibling combinator | `Existing style model` | `style` | `Combinator::Sibling` can receive CSS subsequent-sibling selectors, but matching depends on root-provided sibling traversal. | Selector lowering and matching integration |
| Selector lists | `New style model needed` | `style` | Style lacks a first-class selector-list receiving model for CSS comma-separated selectors and list-specific invalidation/cache keys. | Selector lowering and matching integration |
| Attribute exists matcher | `Existing style model` | `style` | `AttributeSelector::Exists` can receive `[name]`, but root must lower CSS attribute names and document attribute normalization is not modeled. | Selector lowering and matching integration |
| Attribute equality matcher | `Existing style model` | `style` | `AttributeSelector::Equals` can receive `[name=value]`, but CSS value normalization and case-sensitivity modifiers are not modeled. | Selector lowering and matching integration |
| Attribute includes matcher | `New style model needed` | `style` | Style lacks whitespace-token attribute matching for `[name~=value]`. | Selector lowering and matching integration |
| Attribute dash-match matcher | `New style model needed` | `style` | Style lacks language/dash-prefix attribute matching for `[name|=value]`. | Selector lowering and matching integration |
| Attribute prefix matcher | `New style model needed` | `style` | Style lacks prefix attribute matching for `[name^=value]`. | Selector lowering and matching integration |
| Attribute suffix matcher | `New style model needed` | `style` | Style lacks suffix attribute matching for `[name$=value]`. | Selector lowering and matching integration |
| Attribute substring matcher | `New style model needed` | `style` | Style lacks substring attribute matching for `[name*=value]`. | Selector lowering and matching integration |
| Attribute case sensitivity | `New style model needed` | `style` | Style lacks document-default, ASCII-insensitive, and explicit-sensitive attribute comparison modes. | Selector lowering and matching integration |
| `:root` | `New style model needed` | `style` | Style lacks a root-node selector fact distinct from generic tree traversal. | Selector lowering and matching integration |
| `:scope` | `Existing style model` | `style` | `SelectorMatchContext::with_scope` provides the selector anchor, and `RuleScope` resolver matching supplies rule-scope anchors for scoped rules. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `:hover` | `Existing style model` | `style` | `StateFlag::Hovered` is an exact runtime fact for `:hover`. | Selector lowering and matching integration |
| `:active` | `Existing style model` | `style` | `StateFlag::Active` is an exact runtime fact for `:active`. | Selector lowering and matching integration |
| `:focus` | `Existing style model` | `style` | `StateFlag::Focused` is an exact runtime fact for `:focus`. | Selector lowering and matching integration |
| `:focus-visible` | `New style model needed` | `style` | Style lacks a focus-visible runtime state separate from focused. | Selector lowering and matching integration |
| `:focus-within` | `Existing style model` | `style` | `StateFlag::FocusWithin` is an exact runtime fact for `:focus-within`. | Selector lowering and matching integration |
| `:disabled` | `Existing style model` | `style` | `StateFlag::Disabled` is an exact runtime fact for `:disabled`. | Selector lowering and matching integration |
| `:enabled` | `New style model needed` | `style` | Style lacks an enabled runtime fact and selector semantics for elements that participate in enabled/disabled state. | Selector lowering and matching integration |
| `:checked` | `Existing style model` | `style` | `StateFlag::Checked` is an exact true-state fact for `:checked`; indeterminate remains separate and unsupported. | Selector lowering and matching integration |
| `:required` | `New style model needed` | `style` | Style lacks form requirement state facts. | Selector lowering and matching integration |
| `:optional` | `New style model needed` | `style` | Style lacks form optionality state facts. | Selector lowering and matching integration |
| `:valid` | `New style model needed` | `style` | Style lacks form validity state facts. | Selector lowering and matching integration |
| `:invalid` | `New style model needed` | `style` | Style lacks form invalidity state facts. | Selector lowering and matching integration |
| `:placeholder-shown` | `New style model needed` | `style` | Style lacks placeholder-visible runtime state facts. | Selector lowering and matching integration |
| `:modal` | `New style model needed` | `style` | Style lacks modal top-layer/dialog state facts. | Selector lowering and matching integration |
| `:fullscreen` | `New style model needed` | `style` | Style lacks fullscreen runtime state facts. | Selector lowering and matching integration |
| `:popover-open` | `New style model needed` | `style` | Style lacks popover-open runtime state facts. | Selector lowering and matching integration |
| `:default` | `New style model needed` | `style` | Style lacks default-button/default-option state facts. | Selector lowering and matching integration |
| `:indeterminate` | `New style model needed` | `style` | Style has checked true-state matching but lacks indeterminate state facts. | Selector lowering and matching integration |
| `:read-only` | `New style model needed` | `style` | Style lacks editability state facts for read-only controls. | Selector lowering and matching integration |
| `:read-write` | `New style model needed` | `style` | Style lacks editability state facts for read-write controls. | Selector lowering and matching integration |
| `:in-range` | `New style model needed` | `style` | Style lacks range-validity state facts. | Selector lowering and matching integration |
| `:out-of-range` | `New style model needed` | `style` | Style lacks range-invalidity state facts. | Selector lowering and matching integration |
| `:first-child` | `Existing style model` | `style` | `PositionSelector::First` supports simple first-child matching, but root still must map CSS structural pseudo-class lowering and traversal policy. | Selector lowering and matching integration |
| `:last-child` | `Existing style model` | `style` | `PositionSelector::Last` supports simple last-child matching, but root still must map CSS structural pseudo-class lowering and traversal policy. | Selector lowering and matching integration |
| `:only-child` | `New style model needed` | `style` | Style lacks an only-child selector distinct from separate first/last matching. | Selector lowering and matching integration |
| `:empty` | `New style model needed` | `style` | Style has a `Node::text` fact but lacks CSS empty-child/text semantics. | Selector lowering and matching integration |
| `:nth-child()` | `Existing style model` | `style` | `PositionSelector::Nth` supports simple child index matching, but CSS `An+B` lowering, odd/even, and `of <selector-list>` semantics need explicit receiving contracts. | Selector lowering and matching integration |
| `:nth-last-child()` | `New style model needed` | `style` | Style lacks reverse child-index matching and CSS `of <selector-list>` filtering. | Selector lowering and matching integration |
| `:first-of-type` | `New style model needed` | `style` | Style lacks type-filtered structural matching over sibling tags. | Selector lowering and matching integration |
| `:last-of-type` | `New style model needed` | `style` | Style lacks reverse type-filtered structural matching over sibling tags. | Selector lowering and matching integration |
| `:only-of-type` | `New style model needed` | `style` | Style lacks only-of-type structural matching over sibling tags. | Selector lowering and matching integration |
| `:nth-of-type()` | `New style model needed` | `style` | Style lacks type-filtered nth matching over sibling tags. | Selector lowering and matching integration |
| `:nth-last-of-type()` | `New style model needed` | `style` | Style lacks reverse type-filtered nth matching over sibling tags. | Selector lowering and matching integration |
| `:not()` | `New style model needed` | `style` | Style lacks selector-list negation matching and invalidation support. | Selector lowering and matching integration |
| `:is()` | `New style model needed` | `style` | Style lacks selector-list alternation matching and specificity/lowering contracts. | Selector lowering and matching integration |
| `:where()` | `New style model needed` | `style` | Style lacks zero-specificity selector-list matching contracts. | Selector lowering and matching integration |
| Relative selectors | `New style model needed` | `style` | Style lacks relative selector-list inputs whose subject is anchored by a leading combinator. | Selector lowering and matching integration |
| `:has()` | `New style model needed` | `style` | Style lacks relative selector matching, dependency tracking, and invalidation for ancestor/subject queries. | Selector lowering and matching integration |
| `::before` | `New style model needed` | `style` | Pseudo-elements require generated-box style targets; they must not be modeled as tree nodes. | Generated content, counters, lists, and marker policy |
| `::after` | `New style model needed` | `style` | Pseudo-elements require generated-box style targets; they must not be modeled as tree nodes. | Generated content, counters, lists, and marker policy |
| `::marker` | `New style model needed` | `style` | Marker pseudo-elements need list-marker style targets and must not be modeled as tree nodes. | Generated content, counters, lists, and marker policy |
| `::selection` | `New style model needed` | `style` | Selection pseudo-elements need highlight/selection style targets and must not be modeled as tree nodes. | Paint, color, and effect property family expansion |
| `::backdrop` | `New style model needed` | `style` | Backdrop pseudo-elements need top-layer style targets and must not be modeled as tree nodes. | Paint, color, and effect property family expansion |
| `::before::marker` | `New style model needed` | `style` | Before-marker pseudo-element sequences need generated marker targets and must not be modeled as tree nodes. | Generated content, counters, lists, and marker policy |
| `::after::marker` | `New style model needed` | `style` | After-marker pseudo-element sequences need generated marker targets and must not be modeled as tree nodes. | Generated content, counters, lists, and marker policy |

## Condition, Layer, Scope, And Environment Ledger

| CSS surface | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- |
| Media query list | `Existing style model` | `style` | `MediaQueryList` stores disjunctive CSS `@media` query lists and evaluates them against `MediaEnvironment`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Typed media query | `Existing style model` | `style` | `TypedMediaQuery` stores media query modifier, `MediaType`, and optional `MediaCondition`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Condition-only media query | `Existing style model` | `style` | `MediaQuery::Condition` receives condition-only media queries independent of `screen`/`print` media types. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media type | `Existing style model` | `style` | `MediaType` matches root-provided output mode facts in `MediaEnvironment`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media modifier `not` | `Existing style model` | `style` | `MediaQueryModifier::Not` provides negated media query semantics. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media modifier `only` | `Existing style model` | `style` | `MediaQueryModifier::Only` provides the explicit compatibility/legacy `only` modifier contract for media queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Typed media query `and` condition attachment | `Existing style model` | `style` | `TypedMediaQuery` attaches optional `MediaCondition` trees to typed media queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media condition list | `Existing style model` | `style` | `MediaConditionList` stores n-ary media condition groups. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media condition `not` | `Existing style model` | `style` | `MediaCondition::Not` provides media condition negation. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media condition `and` | `Existing style model` | `style` | `MediaCondition::And` provides media condition conjunction. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media condition `or` | `Existing style model` | `style` | `MediaCondition::Or` provides media condition disjunction. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media feature queries | `Existing style model` | `style` | `MediaFeatureQuery` covers root-provided facts in `MediaEnvironment`, including size, resolution, color, preference, forced-colors, hover, pointer, and display-mode facts. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container names | `Existing style model` | `style` | `ContainerName` and `ContainerFacts` preserve selected container identity for root-lowered container query matching. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container condition lists | `Existing style model` | `style` | `ContainerConditionList` stores n-ary container condition groups. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container condition `not` | `Existing style model` | `style` | `ContainerCondition::Not` provides container condition negation. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container condition `and` | `Existing style model` | `style` | `ContainerCondition::And` provides container condition conjunction. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container condition `or` | `Existing style model` | `style` | `ContainerCondition::Or` provides container condition disjunction. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container feature queries | `Existing style model` | `style` | `ContainerFeatureQuery` covers width, height, inline-size, block-size, aspect-ratio, and orientation facts from `ContainerFacts`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container style queries | `Existing style model` | `style` | `ContainerStyleQuery` evaluates style queries against custom property facts in `ContainerFacts`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container style custom property presence query | `Existing style model` | `style` | `ContainerStyleQuery::CustomPropertyPresence` matches custom property presence in `ContainerFacts`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container style custom property value query | `Existing style model` | `style` | `ContainerStyleQuery::CustomPropertyValue` matches authored custom property token values in `ContainerFacts`. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container width | `Existing style model` | `style` | `ContainerFacts` carries CSS query width as `QueryLength` and `ContainerFeatureQuery::Width` evaluates generic range comparisons. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container height | `Existing style model` | `style` | `ContainerFacts` carries CSS query height as `QueryLength` and `ContainerFeatureQuery::Height` evaluates generic range comparisons. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container inline-size | `Existing style model` | `style` | `ContainerFacts` carries inline-size as `QueryLength` and `ContainerFeatureQuery::InlineSize` evaluates it. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container block-size | `Existing style model` | `style` | `ContainerFacts` carries block-size as `QueryLength` and `ContainerFeatureQuery::BlockSize` evaluates it. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container aspect-ratio | `Existing style model` | `style` | `ContainerFacts` carries aspect-ratio as `Ratio` and `ContainerFeatureQuery::AspectRatio` evaluates range comparisons. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Container orientation | `Existing style model` | `style` | `ContainerFacts` carries or derives `Orientation` and `ContainerFeatureQuery::Orientation` evaluates it. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media width | `Existing style model` | `style` | `MediaEnvironment` carries CSS query width as `QueryLength` and `MediaFeatureQuery::Width` evaluates generic range comparisons. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media height | `Existing style model` | `style` | `MediaEnvironment` carries CSS query height as `QueryLength` and `MediaFeatureQuery::Height` evaluates generic range comparisons. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media resolution | `Existing style model` | `style` | `MediaEnvironment` carries `Resolution` facts and `MediaFeatureQuery::Resolution` evaluates dpi/dpcm/dppx ranges. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media color | `Existing style model` | `style` | `MediaEnvironment` carries color-depth facts and `MediaFeatureQuery::Color` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media monochrome | `Existing style model` | `style` | `MediaEnvironment` carries monochrome bit-depth facts and `MediaFeatureQuery::Monochrome` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Media orientation | `Existing style model` | `style` | `MediaEnvironment` carries or derives viewport `Orientation` and `MediaFeatureQuery::Orientation` evaluates it. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `prefers-color-scheme` | `Existing style model` | `style` | `MediaEnvironment` carries `ColorSchemePreference` facts and `MediaFeatureQuery::PrefersColorScheme` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `prefers-reduced-motion` | `Existing style model` | `style` | `MediaEnvironment` carries `ReducedMotionPreference` facts and `MediaFeatureQuery::PrefersReducedMotion` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `prefers-reduced-transparency` | `Existing style model` | `style` | `MediaEnvironment` carries `ReducedTransparencyPreference` facts and `MediaFeatureQuery::PrefersReducedTransparency` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `prefers-contrast` | `Existing style model` | `style` | `MediaEnvironment` carries `ContrastPreference` facts and `MediaFeatureQuery::PrefersContrast` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `forced-colors` | `Existing style model` | `style` | `MediaEnvironment` carries `ForcedColorsMode` facts and `MediaFeatureQuery::ForcedColors` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `hover` | `Existing style model` | `style` | `MediaEnvironment` carries primary `HoverCapability` facts and `MediaFeatureQuery::Hover` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `any-hover` | `Existing style model` | `style` | `MediaEnvironment` carries aggregate `HoverCapability` facts and `MediaFeatureQuery::AnyHover` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `pointer` | `Existing style model` | `style` | `MediaEnvironment` carries primary `PointerCapability` facts and `MediaFeatureQuery::Pointer` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `any-pointer` | `Existing style model` | `style` | `MediaEnvironment` carries aggregate `PointerCapability` facts and `MediaFeatureQuery::AnyPointer` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| `display-mode` | `Existing style model` | `style` | `MediaEnvironment` carries `DisplayMode` facts and `MediaFeatureQuery::DisplayMode` evaluates them. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Range feature without comparison | `Existing style model` | `style` | `RangeFeature<T>` stores an optional `QueryComparison`; omitted comparison uses equality-style range matching. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Range comparison less-than | `Existing style model` | `style` | `QueryComparison::LessThan` provides generic `<` range comparison queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Range comparison less-than-or-equal | `Existing style model` | `style` | `QueryComparison::LessThanOrEqual` provides generic `<=` range comparison queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Range comparison equal | `Existing style model` | `style` | `QueryComparison::Equal` provides exact equality range comparison queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Range comparison greater-than-or-equal | `Existing style model` | `style` | `QueryComparison::GreaterThanOrEqual` provides generic `>=` range comparison queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Range comparison greater-than | `Existing style model` | `style` | `QueryComparison::GreaterThan` provides generic `>` range comparison queries. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Scoped rule order | `New style model needed` | `style` | `RuleScope` and `ScopeSelectorList` now model and match scoped rule anchors; full scoped cascade proximity ordering remains deferred. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Layer order | `Existing style model` | `style` | `LayerRegistry`, `LayerStatement`, `LayerBlock`, and `LayerOrder` model named and anonymous cascade layer ordering. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |
| Unlayered order | `Existing style model` | `style` | `LayerOrder::default()` keeps unlayered rules explicitly ordered apart from registered layered rules. | Operation 14 resolver cache, invalidation, and diagnostic reporting integration |

## Symbolic Data Ledger

| Symbolic surface | Classification | Owner | Style implication | Later plan |
| --- | --- | --- | --- | --- |
| Custom property names | `New style model needed` | `style` | Style needs a custom property name type/store compatible with CSS `--*` names without importing CSS syntax types. | Custom properties and variable substitution |
| Custom property authored values | `New style model needed` | `style` | Style needs to preserve authored custom property token text and nested variable references until computed-value time. | Custom properties and variable substitution |
| Variable references | `New style model needed` | `style` | Style needs a `var(...)` reference graph over custom property names. | Custom properties and variable substitution |
| Variable fallbacks | `New style model needed` | `style` | Style needs fallback authored-value preservation and recursive variable reference tracking for `var(name, fallback)`. | Custom properties and variable substitution |
| Variable-dependent ordinary declaration values | `New style model needed` | `style` | Style needs a receiving path for ordinary property values whose final typed value depends on variable substitution. | Custom properties and variable substitution |
| `currentColor` | `Typed symbolic style data` | `style` | Style should preserve `currentColor` as symbolic color data until property-aware computed color resolution. | Paint, color, and effect property family expansion |
| System colors | `Typed symbolic style data` | `style` | Style should preserve system color identifiers until host/theme environment resolution. | Paint, color, and effect property family expansion |
| `hsl()` | `Typed symbolic style data` | `style` | Style should preserve typed HSL color data instead of collapsing symbolic color syntax to RGBA. | Paint, color, and effect property family expansion |
| `hwb()` | `Typed symbolic style data` | `style` | Style should preserve typed HWB color data instead of collapsing symbolic color syntax to RGBA. | Paint, color, and effect property family expansion |
| `lab()` | `Typed symbolic style data` | `style` | Style should preserve typed Lab color data until color-space conversion policy exists. | Paint, color, and effect property family expansion |
| `lch()` | `Typed symbolic style data` | `style` | Style should preserve typed LCH color data until color-space conversion policy exists. | Paint, color, and effect property family expansion |
| `oklab()` | `Typed symbolic style data` | `style` | Style should preserve typed Oklab color data until color-space conversion policy exists. | Paint, color, and effect property family expansion |
| `oklch()` | `Typed symbolic style data` | `style` | Style should preserve typed Oklch color data until color-space conversion policy exists. | Paint, color, and effect property family expansion |
| `color()` with predefined color spaces | `Typed symbolic style data` | `style` | Style should preserve predefined color-space components rather than coercing to sRGB/RGBA at lowering time. | Paint, color, and effect property family expansion |
| `color-mix()` | `Typed symbolic style data` | `style` | Style should preserve interpolation space, hue interpolation, component colors, and percentages until color resolution. | Paint, color, and effect property family expansion |
| Relative colors | `Typed symbolic style data` | `style` | Style should preserve relative color source and component expressions until color resolution. | Paint, color, and effect property family expansion |
| Color component expressions with variable references | `Typed symbolic style data` | `style` | Style should preserve authored color component expressions and variable references until substitution and color resolution. | Custom properties and variable substitution |
| Transform function arguments | `Typed symbolic style data` | `style` | Style should preserve authored transform function arguments where current typed transform values cannot fully resolve CSS syntax. | Paint, color, and effect property family expansion |
| Filter function arguments | `Typed symbolic style data` | `style` | Style should preserve authored filter function arguments until a typed effect model and resource policy exist. | Paint, color, and effect property family expansion |
| Basic shape arguments | `Typed symbolic style data` | `style` | Style should preserve authored basic-shape arguments for clip-path until geometric resolution exists. | Paint, color, and effect property family expansion |
| Easing function arguments | `Typed symbolic style data` | `style` | Style should preserve authored cubic-bezier and steps arguments until timing-function resolution exists. | Timing, animation, and keyframe style data |

## Current Style Surface Summary

| Style surface | Current role | Gap against CSS surface |
| --- | --- | --- |
| `Property` | Non-exhaustive style-owned property enum with canonical/shorthand metadata, defaults, inheritance, interpolation, and invalidation impact. | Covers the existing Rust style property set, but does not cover the full `CssProperty` ledger, custom properties, CSS cascade metadata, or several CSS longhands and shorthands accepted by CSS. |
| `Value` | Typed style value enum used by declarations, validation, interpolation, hashing, and resolved outputs. | Represents many non-variable layout, text, paint, and effect values, but lacks authored CSS token preservation, variable-dependent values, custom property values, `revert`/`revert-layer`, and many CSS value families recorded as new style models. |
| `Declaration` | Validated `Property` plus `Value` pair for style-owned declarations. | Does not carry CSS source location, importance, origin, layer, specificity, custom property names, or variable-dependent authored values needed for CSS lowering and cascade. |
| `Declarations` | Canonicalized collection of declarations with replacement semantics, shorthand expansion, lookup, iteration, and fingerprinting. | Loses authored declaration ordering and duplicates by canonical property, and lacks CSS cascade buckets for `!important`, custom properties, CSS-wide keywords beyond current resolution, and variable substitution state. |
| `TypedDeclaration` | Small typed front-door constructor wrapper for selected Rust-authored declarations such as width, opacity, text color, and transition duration. | Far narrower than the CSS declaration surface; CSS lowering still needs a comprehensive typed receiving API across all supported properties and symbolic authored values. |
| `Sheet` | Ordered rule store with selector indexing, versioning, conditional-rule iteration, candidate lookup, `LayerRegistry` state, sheet layer APIs for `LayerStatement`/`LayerBlock` lowering, scoped rule APIs, keyframes, and condition-driven invalidation helpers. | Still does not model stylesheet import loading, font-face loading, CSS source diagnostics, cascade origins, `!important` separation, or Operation 14 generalized resolver cache/invalidation/diagnostic reporting. |
| `Rule` | Style rule containing one selector or selector list, legacy or authored declarations, `Condition::Media`/`Condition::Container` lists, optional `RuleScope`, `RulePrecedence` with specificity/layer/source order, style bucket, and source-order policy. | Still lacks cascade origin, `!important` separation, source locations, nested at-rule diagnostic provenance, and scoped cascade proximity ordering/general dependency-index precision deferred to Operation 14. |
| `Selector` | Style-owned matcher over tags, classes, keys, states, attributes, positions, compounds, and complex parts against `Tree` facts. | Does not cover full CSS selector syntax including selector lists, namespaces, richer attribute operators/flags, specificity accounting, pseudo-elements, `:not()`/`:is()`/`:where()`/`:has()`, type-filtered structural selectors, or relative selectors. |
| `Tree` | Root-provided trait exposing node facts and canonical/projected traversal for selector matching and resolver cache keys. | Provides only the current style fact contract; CSS matching needs additional facts or policies for namespaces, language/direction, pseudo-element targets, empty/text semantics, type-filtered sibling matching, shadow/slot boundaries, and `:has()` dependency tracking. |
| `Context` | Resolver input carrying tree/node, traversal mode, `ConditionFacts` (`MediaEnvironment` plus optional `ContainerFacts`), selector root/scope anchors, style bucket, parent resolved style, local declarations, and animated declarations. | Still lacks cascade origin inputs, `!important` separation, root lowering diagnostics, and Operation 14 generalized cache/invalidation integration over environment/container/scope/layer dependencies. |
| `Resolver` | Applies candidate sheet rules, `Condition::Media`/`Condition::Container` evaluation, `RuleScope` matching, layer/specificity/source ordering, inheritance, local declarations, animated declarations, CSS-wide keyword/custom property resolution, and cache management to produce `Resolved`. | Operation 14 still needs generalized resolver cache keys, invalidation, diagnostic reporting integration, scoped cascade proximity ordering, and finer dependency-index precision for selectors, environment/container feature changes, layers, scopes, and custom property dependencies. |
| `Resolved` | Computed style map storing every canonical property with default/inherited values and typed convenience accessors. | Does not expose computed custom properties, unresolved symbolic color/value data, pseudo-element resolved targets, layer/origin provenance, or CSS property families still absent from `Property`/`Value`. |
| `Invalidation` | Property-impact flags for layout, paint, text, effect, and animation changes, plus diffing between resolved styles. | Tracks property-level output changes, but CSS integration needs selector dependency invalidation for advanced selectors, custom property dependency graphs, environment/media/container feature changes, layer/scope changes, and pseudo-element target invalidation. |
| `Change` | Rematch plus invalidation and node/sibling/descendant scope summary derived from property changes or conditions. | Lacks CSS-specific change reasons for class/attribute/state selector dependencies, structural selector dependencies, `:has()` ancestor effects, custom property substitutions, media/container query feature changes, and layer/scope order changes. |
| `Condition` | Style-owned conditional rule model with `Condition::Media(MediaQueryList)` and `Condition::Container(ContainerCondition)`, evaluated against `ConditionFacts`. | Operation 14 still needs generalized resolver cache/invalidation/diagnostic reporting over condition dependencies; host environment discovery and root lowering diagnostics remain outside this crate. |
| `MediaEnvironment` | Root-provided media facts for `MediaQueryList`, including media type, query lengths, length basis, resolution, color/monochrome, orientation, color scheme, reduced motion/transparency, contrast, forced colors, hover, pointer, and display mode. | Operation 14 still needs generalized environment-change invalidation and diagnostic reporting integration; host capability discovery remains root/host owned. |
| `ContainerFacts` | Root-provided container facts for `ContainerCondition`, including optional `ContainerName`, size facts, inline/block size, length basis, aspect ratio, orientation, and custom property style-query facts. | Operation 14 still needs generalized container dependency invalidation, query-container selection diagnostics, and dependency-index precision; container layout/size discovery remains root/layout owned. |
| `Color` | RGBA color value with finite validation and `peniko::Color` conversion. | Does not preserve CSS symbolic color surfaces such as `currentColor`, system colors, HSL/HWB/Lab/LCH/Oklab/Oklch, `color()`, `color-mix()`, relative colors, missing components, or variable-dependent color components. |
| `Length` | Style length enum for normal, px, percent, calc, fill/fit/min/max-content, and auto. | Does not preserve authored CSS units beyond px/percent, viewport/container/font-relative units, math functions beyond simple `CalcLength`, anchor/env variables, or context-dependent symbolic length expressions. |
| `CalcLength` | Style-owned length calc tree with px, percent, and additive/subtractive sums plus serialization and validation. | Handles only linear px/percent sums; CSS math needs unit algebra, multiplication/division, min/max/clamp, nested numeric functions, variable/env references, and non-length calc dimensions. |
| `TextValue` | Aggregate text style output for font family/size/weight/style, line height, color, alignment, wrapping, decoration, and selection color. | Acts as a resolved aggregate, not a full CSS text/font receiving surface; many CSS text/font longhands and shorthands remain missing or keyword-backed, including font-stretch, variants/features, spacing, text transform, text-indent, vertical-align, and detailed decoration longhands. |
| `Transform` | Typed transform list with validated operations used for interpolation and resolved transform access. | Does not preserve the full authored CSS transform function surface, symbolic/variable-dependent arguments, 3D matrix perspective details beyond current ops, transform-box, motion path, or filter/basic-shape effect functions. |

Dependency check: `rg -n "surgeist_css|surgeist-css" Cargo.toml src tests plans`
reported references only in planning files. `Cargo.toml`, `src`, and `tests`
have no `surgeist-css` or `surgeist_css` references.

Rust checks were skipped because this plan creates a source-derived planning ledger only and does not modify Rust source.

## Coverage Audit

| Surface | Expected source count or minimum | Ledger row count | Audit result |
| --- | --- | --- | --- |
| `CssRule` | Source count `9` | `9` | Pass: ledger row count equals source count. |
| `CssValue` | Source count `110` | `110` | Pass: ledger row count equals source count. |
| `CssGlobalKeyword` | Required count `5` | `5` | Pass: ledger row count equals required CSS-wide keyword count. |
| `CssProperty` | Source count `180` | `180` | Pass: ledger row count equals source count. |
| Selector required rows | Minimum `65` | `65` | Pass: ledger row count meets the required minimum. |
| Condition/layer/scope/environment required rows | Minimum `52` | `52` | Pass: ledger row count meets the required minimum. |

## Root Coordination Questions

1. Does root want style to model cascade origin in the first authored-style pass, or only author-origin rules?
2. Which parsed CSS properties should root reject initially even though `surgeist-css` accepts them?
3. Should style expose unsupported-integration diagnostics directly, or should root convert style validation failures into integration diagnostics?
4. Which media, container, and environment facts can root provide to style in the first integration pass?
5. Should `@font-face` descriptors become style-owned symbolic data now, or remain root/text-owned until font loading exists?

## Next Sequence Context

After Operation 13 lands, the next implementation plan should cover Operation
14: resolver cache keys, invalidation, and diagnostic reporting integration over
the now-real authored/property/selector/condition/layer/scope models.
